defaults:
  - backbone: croco

name: splatweaver

opacity_mapping:
  initial: 0.0
  final: 0.0
  warm_up: 1

num_monocular_samples: 32
num_surfaces: 1


gaussian_adapter:
  gaussian_scale_min: 0.2
  gaussian_scale_max: 15.0
  sh_degree: 2

d_feature: 32

visualizer:
  num_samples: 8
  min_resolution: 256
  export_ply: false

apply_bounds_shim: true

gs_params_head_type: dpt_gs
pose_free: true
pretrained_weights: ""
scale_align: false

anchor_feat_dim: 55 # 32
color_attr: 3D # 3D or RGB
mlp_type: unified
scaffold: true